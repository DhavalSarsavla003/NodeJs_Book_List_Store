<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
    <style>
        body {
            background-image: url('https://images.pexels.com/photos/807598/pexels-photo-807598.jpeg');
            background-attachment: fixed;
            background-size: cover;
            background-repeat: no-repeat;
            height: 100%;
        }

        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.2);
        }

        .primary-bg {
            background-color: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(5px);
            color: white;

        }

        .primary-bg input {
            background-color: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            color: white;
        }

        .form-control::placeholder {
            color: rgba(255, 255, 255,0.8);
        }
    </style>
</head>

<body>
    <header class="primary-bg">
        <div class="container">
            <div class="row text-center">
                <div class="logo">
                    <h1 class="display-4 fw-bold py-2">
                        ADD BOOKS LIST
                    </h1>
                </div>
            </div>
        </div>
    </header>
    <main>
        <section style="padding-top: 20px;">
            <div class="container">
                <div class="row justify-content-center">

                    <div class="col-12 col-md-8 col-xl-4 mb-5 mb-xl-0">
                        <div class="backgroundOfBtns primary-bg d-flex flex-wrap justify-content-center align-items-center rounded-4"
                            style="padding: 30px;">
                            <form class="w-100" action="<%=(typeof editBook)!=='undefined' && editBook ? /ubdateBook/ + editBook.id : 'views'%>" method="post">
                                <input type="text" name="id" value="<%=books.id%>" hidden>
                                <input class="form-control primary-bg " type="text" name="name"
                                    placeholder="Enter Name"
                                    value="<%= (typeof editBook !== 'undefined' && editBook) ? editBook.name : '' %>" required/>
                                <input class="form-control primary-bg mt-3" type="text" name="author"
                                    placeholder="Enter Author"
                                    value="<%= (typeof editBook !== 'undefined' && editBook) ? editBook.author : '' %>" required/>
                                <input class="form-control primary-bg  mt-3" type="number" min="1" max="10000" name="price"
                                    placeholder="Enter Price"
                                    value="<%= (typeof editBook !== 'undefined' && editBook) ? editBook.price : '' %>" required />
                                
                                <input style="padding-bottom: 100px;" type="address" class="form-control primary-bg  mt-3" name="description" id="" placeholder="Enter Description" 
                                value="<%= (typeof editBook !== 'undefined' && editBook) ? editBook.description : '' %>" required />
                                
                                <button class="btn primary-bg mt-4" type="submit">
                                   <%= (typeof editBook !== 'undefined' && editBook) ? 'Update Book' : 'Add Book' %>
                                </button>
                            </form>
                        </div>
                    </div>

                    <div class="col-12 col-md-8 col-xl-8 d-flex flex-wrap ">
                        <% if(books.length> 0){ %>
                            <% books.forEach(book=> { %>
                                <div class="col-5 m-2 mb-3">
                                    <div class="backgroundOfBtns primary-bg d-flex flex-wrap justify-content-start align-items-center rounded-4"
                                        style="padding: 20px;border: rgba(255, 255, 255,0.4) solid 1px;" >
                                        <div class="book w-100">
                                            <h2 class="display-6 fw-bolder text-wrap flex-wrap">
                                                <%= book.name %>
                                            </h2>
                                            <hr>
                                            <p>
                                                Author : <%= book.author %>
                                            </p>
                                            
                                            <p>
                                               <%= book.description%>
                                            </p>

                                            <p>
                                                ₹ <%= book.price %>
                                            </p>
                                            <div class="col text-center">
                                                <a href="/editBook/<%= book.id%>" style="transition: all ease-in 0.2s;" class="btn shadow rounded-5 px-4 primary-bg"><i class="bi bi-pencil"></i></a>
                                                
                                                <a href="/viewBook/<%= book.id %>" style="transition: all ease-in 0.2s;" class="btn shadow rounded-5 px-4 primary-bg"><i class="bi bi-book-half"></i></a>

                                                <a href="/deleBook/<%= book.id%>" style="transition: all ease-in 0.2s;" class="btn shadow rounded-5 px-4 primary-bg"><i class="bi bi-trash3"></i>
                                                    </a>
                                                </div>
                                        </div>
                                    </div>
                                </div>
                                <% }) %>
                                    <% }else{ %>
                                        <div class="backgroundOfBtns primary-bg d-flex flex-wrap justify-content-center align-items-center rounded-4 display-6 p-5 w-100">
                                            <p class="text-white">No books found.. Please Add Book...</p>
                                        </div>
                                        <% } %>
                    </div>
                </div>
            </div>
        </section>
    </main>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"></script>

</body>

</html>